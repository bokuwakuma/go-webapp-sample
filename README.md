# go-webapp-sample
Go with gorilla

## Getting started
```sh
$ mkdir workspace
$ cd workspace
$ git init .
$ git remote add origin {your remote repository}
$ go mod init github.com/{your name}/{your repository}
```
